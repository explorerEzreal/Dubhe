## 计划:建立 shibawork.md 云服务器配置记录文档

1. **将 `shibawork.md` 加入 `.gitignore`**,避免服务器 IP、SSH 密码等敏感信息进入 git 历史(文件当前为空,尚未被跟踪,现在加入最干净)。

2. **在 `shibawork.md` 中建立中文文档结构模板**,便于分类记录:
   - 服务器基本信息(云厂商、地域、系统版本、实例规格、公网/内网 IP)
   - SSH 访问方式(地址、端口、用户、认证方式)
   - 已部署服务(部署目录、启动方式、端口、访问入口)
   - 数据库与中间件
   - 常用运维命令
   - 变更记录(按日期追加)

3. **后续使用方式**:你口述配置信息,我按章节记录到该文档;以后凡是涉及云服务器的操作任务,我会先读取 `shibawork.md` 获取连接与部署信息再执行。