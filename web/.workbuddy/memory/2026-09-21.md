# 2026-09-21

## usage-records 页面 UI 重设计（/usage-records）
- 重写 `src/pages/usage-records/` 下 4 个文件，新增 `record-utils.ts`、`UsageRecordSummary.tsx`、`UsageRecordsPage.less`
- 设计：卡片化布局（汇总指标条 + 筛选卡片 + 表格卡片），全部使用 ThemeSync 的 CSS 变量，亮/暗主题均已浏览器实测
- 状态列改为彩色徽标（completed→成功/failed→失败/timeout→超时等，兼容大小写与历史值）
- 表格新增：时间相对值副行、Token 入/出拆分、推理强度标签、行展开详情（requestId/输入输出 Token/耗时/各类 ID）
- 筛选新增设备、用户两个维度（复用 facets.devices/users），增加日期快捷段（今天/近7天/近30天/自定义）、刷新按钮、活跃筛选 chips、清空筛选
- 修复已知缺陷：facets 原先只在 page===1 返回导致翻页后下拉清空 → 改为 mergeFacets 只增不减 + 每次 include:true
- 汇总指标复用 /api/usage-analytics（overview），失败时静默隐藏不阻塞表格
- 技术栈：antd6 + 普通 less（非 module）+ CSS 变量，页面命名空间 `.usage-records-page`
- 验证：tsc/eslint 通过；用临时 preview.html + src/__preview.tsx 挂 mock 数据截图验证亮/暗主题与行展开（已删除临时文件）

### usage-records 表格二次调整（plan mode）
- 用户要求：去掉行展开（+号），改为「两侧固定、中间滚动」：左固定「用户/分组」，右固定「时间、Token、状态」（状态必须最后一列，否则 antd has-fix-end 阴影容器不生效），中间补「输入 Token / 输出 Token / 总耗时」列，Request ID 收进时间列 Tooltip
- 关键坑：antd v6 固定列类名是 `ant-table-cell-fix-start/-end`（不是 v5 的 -left/-right）；固定单元格不透明底色由 antd `style/fixed.js` 的 `.ant-table-wrapper .ant-table-cell-fix { background: tableBg }` 提供，若自己覆盖 thead th 的 background（如 var(--hover-bg) 带 alpha）会穿透 → 已删除该声明，交给 antd 不透明 headerBg(colorFillAlterSolid)
- 已验证：滚动到最右时固定表头/固定列无穿透，行 hover 在固定列上正常，Tooltip 正常；亮/暗主题截图均通过（.workbuddy/preview/usage-records-fixed-*.png）

### usage-records 表格三次微调
- 「用户/分组」拆成两列：用户 190px 左固定，分组 150px 归中间滚动区（带「已停用」小标签）
- 时间列压成单行，删除「X 分钟前」相对时间（formatRelative 已从 record-utils 删除）
- 所有列显式固定宽度（合计 1478，scroll.x=1478），单元格统一 white-space:nowrap + 超出省略号 + 原生 title；固定列用 `:not(.ant-table-cell-fix)` 排除 overflow:hidden，避免裁掉 antd 固定列边界阴影

## 主题系统 v2 重构（翡翠青 + 深青墨 + 鲜明现代）
- 用户诉求：浅色/深色太单调，要更鲜明。确认方向：翡翠青 Teal 主色 / 带色偏深墨深色基底 / 鲜明现代视觉强度
- 核心新增 `src/styles/theme.ts`：全系统唯一色彩来源。三路消费 —— providers.tsx(antd token) / toCssVars()(CSS 变量) / useChartTheme()(ECharts)
- 新增 `src/hooks/useChartTheme.ts`：`useChartTheme()` 图表主题 + `useStatusTone()` 设备状态色
- providers.tsx 重写：antd token 补齐（品牌/表面层级/文字/边框/填充/语义色）+ components 级 token（Layout/Card/Button/Table/Input/Select/Tag/Tooltip/Modal 等）；ThemeSync 改用 useLayoutEffect + data-theme 属性，避免切换闪白
- global.less：logo/头像改品牌渐变 + 品牌阴影；侧栏选中项加 3px 渐变指示条（::before + shadow-brand）；卡片加柔和阴影；硬编码色全部变量化
- ECharts 全接入主题：TrendCard/DistributionCard/monitor-components 的系列色、坐标轴、分割线、tooltip 随模式切换；TrendCard 图例 class 由 blue/green/red 改为 teal/cyan/red
- 语义色收敛：success 原先 3 个版本（#12b76a/#16a34a/#35d399）统一为 #059669(浅)/#34D399(深)
- monitor-components 的 `OverviewCards dark` prop 已移除（改用 hook），DeviceAgentsPage 同步去掉 useAppTheme
- 可访问性修正：三级文字原 #94A3B8 仅 2.4:1 不达标 → 改 #5A6F6C(浅,5.0:1)/#7E968F(深,5.4:1)
- 产出：`docs/theme-spec.md`（设计规范）、`docs/theme-preview.html`（可切换明暗的效果预览）
- 验证：tsc --noEmit / eslint / vite build 全部通过
- 坑：并行对同一文件发多个 Edit 会报 "File has been modified since read"，需串行
